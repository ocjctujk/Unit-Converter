import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.unitconvert.com',
  appName: 'Unit Converter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
